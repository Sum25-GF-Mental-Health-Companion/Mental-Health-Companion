version: "2"
sql:
  - engine: "postgresql"
    queries: "backend/db/queries/queries.sql"
    schema: "backend/db/queries/schema.sql"
    gen:
      go:
        package: "queries"
        out: "backend/db/queries"
        emit_json_tags: true
        emit_interface: false
